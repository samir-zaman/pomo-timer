# pomo-timer
a pomodoro timer that tracks study/work hours on a calendar heat map

Next steps: 
- instead of resetting the current session, the reset button resets back to work session 1. Update handleReset function. 
- Home link
- Pause button resets session instead of pausing
- Replace button layout: one start button, replaced by two buttons upon clicking - pause and reset. 
- Dark mode
- Sounds
