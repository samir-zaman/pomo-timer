// Space for Scratch work
12 / 12 next steps:
- create a sub-collection called study_days, with each date as a document, and date and count data fields. 
- if today's date doesn't match the last entry's date then create a new Firebase object for today in the PomodorTimer component
- import Firebase data
- create heatmap


12/20
-every time the user logs in Firebase is incremented by 25 minutes. I tested this by logging in and out 4 times - Firebase data was increased by 100 minutes. 